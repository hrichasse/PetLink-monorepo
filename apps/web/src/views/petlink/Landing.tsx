import { Link } from "react-router-dom";
import { useQuery } from "@tanstack/react-query";
import { ArrowRight, CalendarCheck, HeartHandshake, PawPrint, Search, ShieldCheck, Star } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PublicNav } from "@/components/petlink/Layout";
import { ServiceCard } from "@/components/petlink/Cards";
import { marketplaceApi } from "@/lib/petlink-api";

export function LandingPage() {
  const { data: services = [] } = useQuery({ queryKey: ["landing-services"], queryFn: () => marketplaceApi.services.list({ isActive: true }) });
  return <div className="min-h-screen bg-background"><PublicNav /><main><section className="relative overflow-hidden"><div className="pointer-events-none absolute inset-0 bg-hero-pattern opacity-70" /><div className="mx-auto grid max-w-7xl gap-10 px-4 py-16 md:grid-cols-[1.05fr_.95fr] md:py-20"><div className="relative z-10 flex flex-col justify-center"><span className="mb-5 w-fit rounded-full bg-primary-soft px-4 py-2 text-sm font-extrabold text-primary">Marketplace confiable para mascotas felices</span><h1 className="max-w-3xl text-5xl font-black leading-tight tracking-normal md:text-7xl">PetLink</h1><p className="mt-5 max-w-xl text-lg leading-8 text-muted-foreground">Conecta dueños con paseadores, guarderías, groomers, veterinarias y cuidadores verificados en una experiencia clara, cálida y rápida.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button asChild size="xl" variant="hero"><Link to="/register">Empezar ahora <ArrowRight className="h-5 w-5" /></Link></Button><Button asChild size="xl" variant="outline"><Link to="/services">Explorar servicios</Link></Button></div></div><div className="relative z-10"><div className="signature-card rounded-[2rem] border bg-card p-5 shadow-warm"><div className="rounded-[1.5rem] bg-gradient-hero p-6"><div className="grid gap-4"><div className="rounded-card bg-card/90 p-4 shadow-soft"><div className="flex items-center justify-between"><strong>Paseo premium</strong><span className="flex items-center gap-1 font-bold"><Star className="h-4 w-4 fill-current text-primary" />4.9</span></div><p className="mt-2 text-sm text-muted-foreground">Milo sale hoy 16:30 con ruta compartida.</p></div><div className="grid grid-cols-2 gap-4"><Metric icon={<PawPrint />} label="Mascotas" value="2" /><Metric icon={<CalendarCheck />} label="Reservas" value="8" /></div><div className="rounded-card bg-card/90 p-4 shadow-soft"><div className="h-28 rounded-2xl bg-petal text-center text-7xl leading-[7rem]">🐶</div></div></div></div></div></div></div></section><section className="mx-auto max-w-7xl px-4 py-12"><div className="grid gap-4 md:grid-cols-3">{[{ icon: ShieldCheck, title: "Confianza", text: "Perfiles, reseñas y estados claros." }, { icon: Search, title: "Búsqueda simple", text: "Filtros por servicio, ciudad y disponibilidad." }, { icon: HeartHandshake, title: "Cuidado cercano", text: "Experiencia pensada para dueños y proveedores." }].map((b) => <article key={b.title} className="rounded-card border bg-card p-6 shadow-soft"><b.icon className="h-8 w-8 text-primary" /><h2 className="mt-4 text-xl font-extrabold">{b.title}</h2><p className="mt-2 text-muted-foreground">{b.text}</p></article>)}</div></section><section className="mx-auto max-w-7xl px-4 py-12"><div className="mb-6 flex items-end justify-between"><div><h2 className="text-3xl font-black">Servicios destacados</h2><p className="text-muted-foreground">Opciones listas para reservar.</p></div><Button asChild variant="soft"><Link to="/services">Ver todos</Link></Button></div><div className="grid gap-5 md:grid-cols-3">{services.slice(0, 3).map((service) => <ServiceCard key={service.id} service={service} />)}</div></section></main></div>;
}

function Metric({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="rounded-card bg-card/90 p-4 shadow-soft"><div className="text-primary">{icon}</div><strong className="mt-3 block text-2xl">{value}</strong><span className="text-sm text-muted-foreground">{label}</span></div>; }
